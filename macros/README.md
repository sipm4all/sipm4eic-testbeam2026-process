# ROOT Macros

This directory contains ROOT analysis helpers and examples for data products written by the processing programs.

```text
example/     executable ROOT macro examples
lib/         reusable macro/helper headers
```

The preferred way to read triggered frame output is through `macros/lib/trigger_reader.h`, which hides the flattened ROOT branch representation.
