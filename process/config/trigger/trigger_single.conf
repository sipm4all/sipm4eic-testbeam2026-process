# Simple zero-offset seed: the raw seed time already represents event time.
selector TRIGGER {
    type        = 1
    device      = 192
    fifo        = 3
    column      = 1
    pixel       = 12
    time_offset = 0.
}

trigger {
    seed TRIGGER
}
