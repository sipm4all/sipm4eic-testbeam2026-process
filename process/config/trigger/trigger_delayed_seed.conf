# Trigger seed only. With calibrated inputs, the frame is centered on the trigger calibrated time.
# The seed hit is still stored exactly once in each accepted frame.
selector TRIGGER {
    type        = 9
    device      = 200
    fifo        = 32
    column      = *
    pixel       = *
    time_offset = 0.
}

trigger {
    seed TRIGGER
}
